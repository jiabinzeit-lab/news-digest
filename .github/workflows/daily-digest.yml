name: Daily News Digest

on:
  schedule:
    # 4:00 UTC = 6:00 CEST (夏令时) / 5:00 CET (冬令时)
    # 提前触发，预留 GitHub 队列延迟，确保9点前送达
    - cron: '0 4 * * *'
  workflow_dispatch: # 允许手动触发

jobs:
  send-digest:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '24'
          cache: 'npm'
          cache-dependency-path: |
            news-aggregator-mcp/package-lock.json
            telegram-push-mcp/package-lock.json

      - name: Install dependencies
        run: |
          npm ci
          cd news-aggregator-mcp && npm ci
          cd ../telegram-push-mcp && npm ci

      - name: Send digest
        env:
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
        run: node daily-digest.mjs
